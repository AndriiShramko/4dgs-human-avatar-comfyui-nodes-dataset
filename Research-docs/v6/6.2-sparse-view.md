<!-- v7-audit-notice -->
> **ℹ️ AUDITED BY V7 (2026-07-12).** This v6 file stays citable for its implementation detail; claim-level verification statuses live in [v7/CLAIMS_REGISTRY](../v7/CLAIMS_REGISTRY.md) and the apex plan is [Research-docs/v7](../v7/README.md). Owner decisions recorded in v6 remain binding.

# Section 2: Sparse-View Generation Strategies (1-3 Cameras)

> **Fact-check audit (2026-06-12):** 31 claims checked, 7 corrected, 3 unverifiable/likely hallucinated. Annotations inline. All cited papers (DiffusionGS, GIFSplat, iLRM, Difix3D+, DiffSplat, HumanSplat, Long-LRM, HiRes-GS, Splat4D, Anny, MHR) were confirmed to exist with the claimed IDs. Main corrections: HumanSplat's full pipeline is ~9.3s (not 0.3s); the "5-15 dB occluded-region PSNR gap" could not be verified; NFE counts are unverified; failure-mode list is sourced from sparse-view 3DGS literature, not LRM-specific studies.

## Executive Summary

The v5 plan contains **two critical overstatements**: (1) that inference-time diffusion is an absolute technological dead-end, and (2) that LRM feed-forward transformers can flawlessly hallucinate occluded regions. Reality in 2025-2026: single-stage diffusion (DiffusionGS, >5x faster than prior two-stage SOTA, ~6s on A100) and single-step diffusion fixers (Difix3D+) make lightweight diffusion refinement feasible at near-real-time latency. Simultaneously, pure feed-forward LRMs produce blurry, averaged textures in occluded zones and suffer from near-field overfitting. The **recommended architecture** is a hybrid: feed-forward LRM base for speed + optional 1-step diffusion refinement for quality, following the GIFSplat/iLRM paradigm.

---

## Audit of v5 Claims

| # | v5 Claim | Status | Evidence |
|---|----------|--------|----------|
| 2.1 | Splat4D inference-time diffusion requires minutes/hours → dead-end for real-time | **Overstated** | Splat4D's (SIGGRAPH 2025) optimization + video-diffusion pipeline is indeed slow ⚠️ [fact-check 2026-06-12: the "20-50 NFE" figure could not be verified in the paper's public materials; treat as unconfirmed]. But DiffusionGS (ICCV 2025) achieves single-stage generation in ~6s on A100. GIFSplat (2026) uses iterative feed-forward with distilled diffusion at second-scale latency. The bottleneck is multi-step iteration, not diffusion fundamentally. |
| 2.2 | LRM (HumanSplat-like) can hallucinate back/occluded regions via single forward pass | **Overstated** | Research 2025-2026 shows feed-forward LRMs produce ghosting artifacts, near-field overfitting, and blurry far-field regions. iLRM and GIFSplat explicitly address this via iterative refinement. Single-pass hallucination quality degrades significantly for complex poses and loose clothing. |
| 2.3 | HumanSplat extracts 3DGS in ~0.3s at >150 FPS rendering | **Misleading** | The 0.3s figure covers only the latent reconstruction transformer. HumanSplat's full pipeline includes a 2D multi-view (video) diffusion stage of ~9s, for ~9.3s total inference on A100. The >150 FPS rendering on A100 is confirmed, but applies to the final compact output, not during generation. |
| 2.4 | SMPL structure priors ensure anatomical correctness | **Confirmed with caveats** | Parametric body models prevent gross anatomical errors. But they constrain the output to model topology — loose clothing, accessories, and unusual body types are poorly represented. Also: SMPL has licensing issues (see Section 6.7). |
| 2.5 | Diffusion is only acceptable in offline/async B2B pipelines | **Incorrect** | 1-step distilled diffusion (Difix3D+, consistency models) operates at <2s latency. This is acceptable for interactive (non-real-time) B2C applications like avatar creation wizards. |

---

## SOTA Analysis 2025-2026

### 2.1 The New Landscape: Beyond Binary "Diffusion vs Feed-Forward"

The 2025-2026 research has dissolved the hard boundary between diffusion-based and feed-forward approaches. Three paradigms now coexist:

**Paradigm A: Pure Feed-Forward (LRM)**
- Long-LRM, SHARP (note: HumanSplat does NOT belong here — its pipeline includes a ~9s 2D multi-view diffusion stage; only its latent reconstruction step runs in 0.3s)
- Latency: ~0.3-1.0s (Long-LRM: ~1s for 32 views on A100; SHARP: <1s on a standard GPU)
- Quality: Good for visible regions, degraded for occluded areas
- Best for: Real-time rendering pipeline initialization

**Paradigm B: Single-Stage Diffusion**
- DiffusionGS (ICCV 2025): Directly outputs 3D Gaussian point clouds at each timestep
- Latency: ~6s on A100 (>5x faster than prior SOTA two-stage methods)
- Quality: +2.20 dB PSNR / 23.25 FID improvement over prior art on objects (+1.34 dB on scenes)
- Best for: Near-real-time quality generation

**Paradigm C: Hybrid Feed-Forward + Refinement**
- GIFSplat (2026): Feed-forward base + iterative residual updates with distilled diffusion priors
- iLRM (2025): Two-stage attention with iterative refinement
- Difix3D+ (2025): Single-step diffusion for artifact cleanup on 3DGS renders
- Latency: 2-10s depending on refinement iterations
- Quality: Approaches optimization-based methods
- Best for: Our MVP — balances speed and quality

### 2.2 DiffusionGS — Single-Stage 3D Diffusion (ICCV 2025, arXiv 2411.14384)

Key innovation: instead of generating 2D images and lifting to 3D, DiffusionGS directly outputs pixel-aligned 3D Gaussian point clouds at each denoising timestep. This enforces view consistency structurally rather than through expensive cross-view attention.

- Input: Single image (text-to-3D is possible only by chaining an external text-to-image model)
- Output: Complete 3D Gaussian scene (objects and scenes; robust to non-object-centric prompt views)
- Speed: ~6 seconds on NVIDIA A100
- Quality: PSNR +2.20 dB over prior art for objects (+1.34 dB for scenes)
- Limitation: Still requires multiple denoising steps ⚠️ [fact-check 2026-06-12: the "~20 NFE" figure could not be verified in public materials; treat as unconfirmed]

### 2.3 GIFSplat — Generative Prior-Guided Iterative Feed-Forward (2026, arXiv 2602.22571)

GIFSplat represents the most promising hybrid architecture for our use case:

1. **Stage 1 (Feed-Forward)**: Fast coarse 3D Gaussian prediction from sparse unposed views
2. **Stage 2 (Iterative Refinement)**: Small forward-only residual updates guided by distilled diffusion priors
3. **Key property**: Maintains second-scale inference while progressively improving quality (paper reports up to +2.1 dB PSNR over feed-forward baselines, no camera poses or test-time gradient optimization required)

Critical insight: refinement uses **residual updates** (small corrections) distilled from a frozen diffusion prior without gradient backpropagation, not full diffusion denoising. This avoids the NFE bottleneck while leveraging diffusion knowledge.

### 2.4 Difix3D+ — Single-Step Diffusion Cleanup (2025)

A pragmatic approach that works with any 3DGS pipeline:

1. Render novel views from initial 3DGS reconstruction
2. Apply single-step image diffusion model (Difix) to clean up artifacts
3. Use cleaned renders as pseudo-ground-truth for fine-tuning the 3DGS
4. Achieves an average ~2x FID improvement over baselines while maintaining 3D consistency; the single model works with both NeRF and 3DGS

This is applicable as a **post-processing module** for any feed-forward output.

### 2.5 DiffSplat — Native Gaussian Diffusion (ICLR 2025)

Adapts large-scale text-to-image diffusion models to natively generate 3D Gaussian splat grids:
- Combines diffusion loss on Gaussian splat grids with 3D rendering loss
- Leverages web-scale 2D priors while maintaining 3D consistency
- Scalable to diverse object categories

### 2.6 Known Limitations of Pure Feed-Forward LRMs

Research from 2025-2026 identifies systematic failure modes (⚠️ fact-check 2026-06-12: items 1-3 are documented primarily in the sparse-view 3DGS literature — D²GS, arXiv 2510.08566, and HiRes-GS — not in LRM-specific studies; floaters near the camera are also documented for pixel-aligned feed-forward methods, e.g. VolSplat, arXiv 2509.19297):

1. **Near-field overfitting**: Excessive Gaussian density near the camera, causing splotchy appearance at close range
2. **Far-field underfitting**: Insufficient coverage in background and distant occluded regions
3. **Resolution trade-off**: High-resolution inputs amplify noise/ghosting; low-resolution inputs lose details
4. **Pose sensitivity**: Quality degrades significantly for unusual poses not well-represented in training data
5. **Temporal inconsistency**: Independent per-frame prediction causes flickering in video sequences

Solutions emerging in 2025-2026:
- **HiRes-GS** (ICLR 2026 submission, under review): Hierarchical multi-scale optimization with matching-based pruning
- **Long-LRM** (arXiv 2410.12781): Mamba2 + transformer blocks with token merging to handle 250K tokens (32 views at 960x540, ~1s on A100)
- **Depth-and-density guided dropout** (D²GS, arXiv 2510.08566): probabilistic Gaussian dropout scored by local density and camera distance

---

## Critical Analysis

### Risk 1: LRM Quality for Back/Occluded Regions

The v5 plan's core assumption — that an LRM can "mathematically hallucinate" high-quality back regions from a single forward pass — is the **highest risk** in the entire architecture.

Evidence against:
- HumanSplat relies on a ~9s multi-view diffusion stage to hallucinate unseen views before its 0.3s latent reconstruction — i.e., even this "fast" method does not get occluded regions from a single feed-forward pass
- Complex clothing patterns (e.g., backpack straps continuing to the back) require semantic understanding that single-pass attention may not capture
- ⚠️ [fact-check 2026-06-12: the claim of a "5-15 dB PSNR gap between visible and occluded regions" could not be verified in any paper; treat as unconfirmed. What is documented: a ~5.7-6.2 dB generalization gap in sparse-view reconstruction overall (PhysicsNeRF, arXiv 2505.23481), which still supports the qualitative point that occluded/underconstrained regions degrade substantially]

**Mitigation**: Plan for 2-stage inference:
1. Stage 1: Feed-forward LRM (~0.5s) → coarse 360° avatar
2. Stage 2: 1-step refinement (~1-2s) → clean up occluded regions using distilled diffusion

Total latency: ~2-3s — acceptable for avatar creation flow.

### Risk 2: Training Data Requirements for Generalization

LRM generalization depends entirely on training data diversity. Our 90-camera studio rig provides excellent multi-view consistency but limited subject diversity compared to internet-scale datasets.

**Mitigation**:
- Pre-train on large-scale 2D datasets (use diffusion teacher)
- Fine-tune on studio rig data for geometric precision
- Data augmentation: synthetic pose variation, clothing simulation

### Risk 3: Parametric Body Model Dependency

Both v5 and most SOTA methods assume SMPL/SMPL-X for structure priors. This creates:
- Licensing risk (see Section 6.7 — solved by Anny/MHR)
- Topology constraint (SMPL assumes fixed mesh topology — fails for loose clothing, accessories)
- Pose estimation errors propagating to 3D reconstruction

**Mitigation**: Use Anny (Apache 2.0) or MHR (Apache 2.0) as parametric prior. Add a non-parametric branch for clothing/accessories that operates outside the body model topology.

---

## Architectural Recommendations for MVP

### Recommended Architecture: Two-Stage Hybrid

```
Input (1-3 cameras) → Body Pose Estimator (Anny/MHR)
                           ↓
                    Feature Encoder (DINOv2)
                           ↓
              ┌────────────┴────────────┐
              │                         │
    Voxel-Aligned LRM            Structure Prior
    (coarse 4DGS, ~0.5s)        (body template)
              │                         │
              └────────────┬────────────┘
                           ↓
                    Merge & Render
                           ↓
                  1-Step Refinement
                  (Difix3D+-style, ~1s)
                           ↓
                  Final 4DGS Avatar
```

### Key Design Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Base generation | Voxel-aligned LRM (not pixel-aligned) | 360° coverage, resolution-independent density |
| Structure prior | Anny or MHR (Apache 2.0) | Commercial safety, competitive quality |
| Refinement | 1-step distilled diffusion | Fixes occluded region artifacts without latency explosion |
| Feature backbone | DINOv2 ViT-L/14 | Best trade-off of semantic richness and speed |
| Training supervision | Rendering loss (L1+LPIPS+DISTS) + depth priors | Proven SOTA approach |
| Total inference budget | <3 seconds on A100 | Acceptable for B2C avatar creation |

---

## Open Questions

1. **Refinement step count**: Is 1 step sufficient, or do we need 2-4 steps for acceptable quality on complex clothing? Requires ablation study.
2. **Diffusion teacher selection**: SV3D vs Zero123++ vs MVDream for multi-view distillation — which provides best quality/speed for human avatars?
3. **Pose estimation robustness**: How does Anny/MHR perform on unusual body types, children, wheelchair users? Edge cases need testing.
4. **Temporal extension**: GIFSplat is designed for static 3D. How to extend the hybrid paradigm to temporal 4D sequences?

---

## References

1. DiffusionGS — "Baking Gaussian Splatting into Diffusion Denoiser for Fast and Scalable Single-stage Image-to-3D Generation and Reconstruction", ICCV 2025, https://arxiv.org/abs/2411.14384 (code: https://github.com/caiyuanhao1998/Open-DiffusionGS)
2. Difix3D+ — "Improving 3D Reconstructions with Single-Step Diffusion Models", CVPR 2025, https://arxiv.org/abs/2503.01774
3. DiffSplat — "Repurposing Image Diffusion Models for Scalable Gaussian Splat Generation", ICLR 2025, https://arxiv.org/abs/2501.16764
4. GIFSplat — "Generative Prior-Guided Iterative Feed-Forward 3D Gaussian Splatting from Sparse Views", 2026, https://arxiv.org/abs/2602.22571
5. iLRM — "An Iterative Large 3D Reconstruction Model", CVPR 2026, https://arxiv.org/abs/2507.23277
6. HumanSplat — "Generalizable Single-Image Human Gaussian Splatting with Structure Priors", NeurIPS 2024, https://proceedings.neurips.cc/paper_files/paper/2024/hash/87affd2029375d1be123ccdab5334c55-Abstract-Conference.html (arXiv: https://arxiv.org/abs/2406.12459)
7. Long-LRM — "Long-sequence Large Reconstruction Model for Wide-coverage Gaussian Splats", https://arxiv.org/abs/2410.12781
8. HiRes-GS — "Hierarchical Resolution Scaling for Sparse-View High-Resolution 3D Gaussian Splatting", ICLR 2026 submission (under review, acceptance unconfirmed as of 2026-06-12), https://openreview.net/forum?id=bg0eG7ycel
9. Anny — "Human Mesh Modeling for Anny Body", NAVER Labs Europe, Apache 2.0, https://github.com/naver/anny (arXiv: https://arxiv.org/abs/2511.03589)
10. MHR — Momentum Human Rig, Meta, Apache 2.0, https://github.com/facebookresearch/MHR (arXiv: https://arxiv.org/abs/2511.15586)
11. Splat4D — "Diffusion-Enhanced 4D Gaussian Splatting for Temporally and Spatially Consistent Content Creation", SIGGRAPH 2025, https://arxiv.org/abs/2508.07557
12. SHARP — "Sharp Monocular View Synthesis in Less Than a Second", Apple, https://arxiv.org/abs/2512.10685 (code: https://github.com/apple/ml-sharp)
13. D²GS — "Depth-and-Density Guided Gaussian Splatting for Stable and Accurate Sparse-View Reconstruction", https://arxiv.org/abs/2510.08566
