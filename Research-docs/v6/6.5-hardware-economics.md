# Section 5: Hardware Economics and Micro-Quantization

> **Fact-check audit (2026-06-12):** 31 claims checked, 9 corrected, 2 unverifiable/likely hallucinated. Annotations inline. Main corrections: "B200 Ultra" → B300/Blackwell Ultra (288 GB), 2026 B200 rental prices ($4–6/hr, not $8–12/hr), NVFP4 BF16-layer recipe details, and weekly/per-run cost arithmetic. Core recommendation (H100/H200 + FP8 first) still stands — see Risk analysis.

## Executive Summary

The v5 plan's hard dependency on NVIDIA Blackwell B200 and NVFP4 format is **premature and unnecessarily risky** for an MVP. While NVFP4 on Blackwell delivers real throughput gains (1.59x over BF16), research reveals that NVFP4 "provably neutralizes traditional outlier mitigation techniques" and requires selective BF16 layers for convergence stability. Meanwhile, **H100/H200 with FP8 Transformer Engine achieves ~99% quality retention** at significantly lower rental cost (~$2-3/hr in 2026 vs ~$4-6/hr for B200 on-demand) ⚠️ [fact-check 2026-06-12: original text claimed $8-12/hr for B200; 2026 market rates are ~$4-6/hr on-demand (Lambda $4.99-5.29, RunPod $5.89) and ~$2-3/hr spot, so the cost gap is ~2x per GPU-hour, not 3-4x]. The recommended strategy: **develop and train MVP on H100/H200 + FP8**, design the architecture to be quantization-friendly, and migrate to B200 + NVFP4 for production scaling only after validating convergence.

---

## Audit of v5 Claims

| # | v5 Claim | Status | Evidence |
|---|----------|--------|----------|
| 5.1 | B200 has 208B transistors, 2.6x Hopper density | **Confirmed** | NVIDIA official specifications. Dual-die (two reticle-limit dies) on TSMC 4NP process. Note: 2.6x refers to transistor *count* vs H100's 80B (same 4nm-class process), not density. |
| 5.2 | NV-HBI interconnect provides 10 TB/s between chiplets | **Confirmed** | Official spec. Enables transparent single-GPU programming model. |
| 5.3 | HBM3e provides 8 TB/s memory bandwidth, up to 288 GB | **Partially confirmed** | B200: 192 GB HBM3e at 8 TB/s (confirmed). The 288 GB configuration is the **B300 / Blackwell Ultra** (12-high HBM3e stacks), a separate product — there is no "B200 Ultra". ⚠️ [fact-check 2026-06-12: corrected product name] |
| 5.4 | NVFP4 block size 16 is "critically important" vs MXFP4 block size 32 | **Overstated** | Smaller blocks reduce quantization error but the improvement is incremental, not fundamental. The real advantage is the E4M3 scale factor (vs E8M0 in MXFP4), which provides 16x finer scale granularity. |
| 5.5 | NVFP4 achieves accuracy equivalent to 16-bit formats | **Marketing claim** | NVIDIA's own paper requires selective BF16 layers for convergence. Independent research (arxiv 2509.23202) shows NVFP4 "provably neutralizes traditional outlier mitigation techniques." Accuracy is near-BF16 but not equivalent. |
| 5.6 | NVFP4 provides 1.59x throughput acceleration | **Confirmed** | MLPerf Training v5.1 and NVIDIA blog benchmarks confirm 1.59x throughput gain for dense matrix operations. |
| 5.7 | NVFP4 is the only viable path for training 4DGS transformers | **Incorrect** | H100/H200 with FP8 Transformer Engine provides ~99% quality retention with 33% inference speedup and 50% KV cache reduction. FP8 training is mature and well-validated. |

---

## SOTA Analysis 2025-2026

### 5.1 NVFP4: Real Capabilities vs Marketing

**Confirmed benefits**:
- 4-bit E2M1 data format with per-block E4M3 scale factor
- Block size 16 (vs MXFP4's 32) → better dynamic range adaptation
- Two-level scaling: per-block scale + per-tensor scale
- 1.59x throughput over BF16 on Blackwell Tensor Cores
- Native hardware support via tcgen05 instructions

**Confirmed problems** (from independent research):

1. **Outlier neutralization**: "NVFP4's small group size provably neutralizes traditional outlier mitigation techniques" (Bridging the Gap, arxiv 2509.23202). Standard clipping/rotation methods that work for INT4 or MXFP4 fail for NVFP4.

2. **Selective BF16 requirement**: NVIDIA's own training recipe for NVFP4 requires keeping certain sensitive layers in BF16 to maintain convergence. In NVIDIA's pretraining paper (arXiv 2509.25149, 12B hybrid Mamba-Transformer trained on 10T tokens), linear layers in the first two and final eight of 62 blocks (~16% of linear layers) were kept in BF16; NVIDIA's general guidance is to keep up to ~15% of final layers in higher precision. The recipe also requires Random Hadamard Transforms, 2D block scaling on weights, and stochastic rounding on gradients. ⚠️ [fact-check 2026-06-12: original said "Llama 3 8B, ~10-15% of layers" — the BF16-layer figure comes from the 12B Mamba-Transformer paper; the Llama 3 8B result is the separate 1.59x throughput benchmark]

3. **MR-GPTQ necessity**: To achieve practical accuracy, block-wise Hadamard transforms and format-specific optimizations (MR-GPTQ) are needed, achieving up to 3.6x layer-wise speedups with proper accuracy.

4. **Not validated for 4DGS**: All published NVFP4 results are on language models (Llama, Mistral). The behavior of NVFP4 on geometric regression (predicting 3D coordinates, quaternions, SH coefficients) is **unknown and unvalidated**.

### 5.2 H100/H200 + FP8: The Pragmatic Alternative

**FP8 Transformer Engine (Hopper architecture)**:
- Native hardware support on H100/H200
- Two FP8 variants: E4M3 (higher precision) and E5M2 (wider range)
- Automatic mixed-precision: FP8 for compute, FP16/BF16 for master weights
- ~99% quality retention across LLM benchmarks (widely reported as "minimal/near-zero degradation"; exact figure varies by model and benchmark)
- ~33% faster inference vs FP16 (per Ref 8 benchmark; other sources report 1.3-2x depending on workload)
- 50% memory reduction for KV cache (FP8 KV cache vs FP16 — confirmed)

**Market economics (2026)**:
| GPU | Rental cost ($/hr) | VRAM | Memory BW | FP8 TFLOPS (dense) |
|-----|-------------------|------|-----------|------------|
| H100 80GB | ~$2-3/hr (spot from ~$1.5; hyperscalers up to ~$7) | 80 GB | 3.35 TB/s | 1,979 |
| H200 141GB | ~$3-4/hr (median ~$3.4; hyperscalers up to ~$10+) | 141 GB | 4.8 TB/s | 1,979 |
| B200 192GB | ~$4-6/hr (spot from ~$2; Lambda $4.99-5.29, RunPod $5.89) | 192 GB | 8 TB/s | 4,500 |

⚠️ [fact-check 2026-06-12: original table listed H200 at $4-5/hr and B200 at $8-12/hr; corrected to mid-2026 market rates. TFLOPS figures are dense FP8 (with 2:4 sparsity: H100/H200 3,958; B200 9,000).]

For MVP development (training runs of days-weeks), the cost difference is meaningful but smaller than originally claimed:
- 1 week single-GPU H100 training: ~$400-600 (at $2.5-3.5/hr × 168 h); an 8-GPU node: ~$3,400-4,700/week
- 1 week single-GPU B200 training: ~$700-1,000 (at $4-6/hr × 168 h) ⚠️ [fact-check 2026-06-12: original said $1,300-2,000, based on outdated $8-12/hr pricing; original "1 week H100 ~$500-700" was a single-GPU figure, now labeled as such]
- **H100 is ~2x cheaper per GPU-hour**; since B200 delivers ~2.3x dense FP8 throughput, the cost *per training run* is roughly comparable — the case for H100 rests on availability, ecosystem maturity, and FP8 training risk, not raw economics ⚠️ [fact-check 2026-06-12: original claim "2-3x cheaper per training run" did not hold at 2026 prices]

### 5.3 The 4DGS-Specific Quantization Challenge

4DGS parameters have unique quantization sensitivity:

| Parameter Group | Dimension | Sensitivity to quantization | Recommended precision |
|----------------|-----------|---------------------------|----------------------|
| Position (μ) | 3 | **Very high** — sub-mm shifts visible | FP16 minimum |
| Quaternion (q) | 4 | **Very high** — small errors cause large rotations | FP16 minimum |
| Scale (s) | 3 | **High** — affects Gaussian footprint | FP8 acceptable |
| Opacity (α) | 1 | **Medium** — binary-like after training | FP8 or lower |
| SH DC (color) | 3 | **Medium** — smooth color space | FP8 acceptable |
| SH AC (high-freq) | 0 (if using MLP) | N/A | N/A (MLP weights) |
| DCT coefficients | 36 | **High** — trajectory errors accumulate | FP16 for first coefficients |

This heterogeneity means **uniform quantization is suboptimal**. A mixed-precision strategy per parameter group is needed regardless of hardware.

### 5.4 TMEM and Asynchronous Tensor Core Execution

Blackwell's TMEM (Tensor Memory) is genuinely innovative:
- Isolated memory pool for tensor core operands
- Eliminates register pressure at FP4 speeds
- Enables asynchronous execution: tensor cores compute while CUDA cores handle activation functions

This is a **real architectural advantage** that FP8 on Hopper cannot replicate. However, for our workload (dominated by attention mechanisms and rendering loss computation), the benefit is approximately 30-40% — meaningful but not transformational. ⚠️ [fact-check 2026-06-12: TMEM itself is confirmed (256 KB per SM, tcgen05 single-thread MMA, relieves register pressure), but the "30-40% benefit for our workload" figure is an unverified internal estimate, not a published benchmark]

### 5.5 Hardware Decompression Engine

Blackwell's on-chip decompression (Snappy, LZ4, Deflate) is valuable for data loading from NVMe storage. However:
- Our 90-camera 4K data can be preprocessed and stored uncompressed in HBM
- The bottleneck for our workload is compute, not data loading
- nvCOMP library provides software decompression on Hopper with minimal CPU overhead

---

## Critical Analysis

### Risk 1: B200 Availability and Pricing Uncertainty

⚠️ [fact-check 2026-06-12: this risk is largely outdated. As of mid-2026, B200 is available on-demand from many neoclouds (Lambda, RunPod, Spheron, Voltage Park, etc.) at ~$4-6/hr, with spot from ~$2/hr. Residual risks:]
- Large *dedicated/reserved* B200 clusters may still have lead times
- Spot instance interruptions remain a real concern for long training runs
- Hyperscaler (AWS/Azure/GCP) B200 pricing remains at a significant premium

H100/H200 is widely available with competitive pricing from multiple providers — but so is B200 in single-node quantities. The availability argument has weakened; the FP8-maturity and NVFP4-convergence-risk arguments (Risk 2) are now the primary justification.

### Risk 2: NVFP4 Convergence for Geometric Regression

All published NVFP4 training results are on language models. The behavior of FP4 quantization on:
- 3D coordinate regression (continuous values in [-10, 10] range)
- Quaternion prediction (unit sphere constraint)
- Rendering loss gradients (flowing through differentiable rasterizer)

is **completely uncharacterized**. Training on B200 with NVFP4 could require significant hyperparameter tuning, selective layer precision, and custom quantization schedules — adding weeks to the development timeline.

### Risk 3: Vendor Lock-in

Hard dependency on Blackwell limits:
- Cloud provider options (fewer B200 instances available)
- Fallback strategies if training diverges
- Cost negotiation leverage

### Risk 4: Over-optimization at Wrong Stage

For MVP, the bottleneck is **algorithm correctness**, not hardware efficiency. Spending engineering time on NVFP4 optimization before validating the core architecture wastes resources.

---

## Architectural Recommendations for MVP

### Two-Phase Hardware Strategy

**Phase 1: MVP Development and Training (H100/H200 + FP8)**
- Use H100 80GB or H200 141GB instances
- FP8 Transformer Engine for all attention layers
- FP16 master weights for position and quaternion parameters
- Mixed precision: FP8 compute + FP16 accumulation
- Estimated cost: $15,000-25,000 for initial training campaign ⚠️ [fact-check 2026-06-12: inconsistent with the Cost Estimate table below (~$57,000 for 3 months on 8×H100); $15-25K corresponds to roughly 1-1.5 months on 8×H100, or 3 months on 2-3 GPUs — clarify which scenario is intended]

**Phase 2: Production Scaling (B200 + NVFP4, optional)**
- Migrate to B200 only after architecture is validated
- Use NVFP4 for inference optimization
- Keep sensitive layers (position, quaternion heads) in FP8/FP16
- Estimated throughput gain: 1.5-2x over Phase 1

### Quantization-Friendly Architecture Design

Design the transformer architecture to be quantization-friendly from the start:

1. **Avoid extreme activation ranges**: Use LayerNorm before every attention block
2. **Smooth weight distributions**: Initialize with small fan-in variance
3. **Per-group quantization hooks**: Design parameter heads with grouped outputs so each group can have independent quantization scale
4. **Gradient checkpointing**: Reduce VRAM usage without quantization artifacts

### Cost Estimate

| Resource | Specification | Monthly cost |
|----------|--------------|-------------|
| Training cluster | 8× H100 80GB | ~$17,000/month (implies ~$2.9/GPU-hr; at $2/hr neocloud rates this drops to ~$11,700/month) |
| Storage | 50 TB NVMe | ~$2,000/month |
| Networking | 400 Gbps InfiniBand | Included |
| **Total MVP training** | **~3 months** | **~$57,000** (= ($17K compute + $2K storage) × 3 — arithmetic checks out) |

vs B200 equivalent: ~$100,000+ for same wall-clock time (8× B200 at ~$5.5/hr ≈ $32K/month × 3). ⚠️ [fact-check 2026-06-12: figure holds at 2026 prices for equal wall-clock time, but B200's ~2.3x FP8 throughput would shorten the run, making effective per-run cost roughly comparable to H100 — see Section 5.2]

---

## Open Questions

1. **FP8 vs BF16 for rendering loss**: Does the rendering loss (differentiable rasterization) degrade with FP8 activations? Requires benchmarking on gsplat with FP8 mode.
2. **Quantization-aware training**: Should we use QAT from the start, or train in FP16 first and quantize post-training?
3. **Multi-node scaling**: For 8+ GPU training, is NVLink (H100 DGX) necessary or can we use PCIe with gradient compression?
4. **B200 availability timeline**: When will B200 cloud instances be readily available at competitive pricing for startups?

---

## References

1. NVIDIA NVFP4 Training Blog — https://developer.nvidia.com/blog/using-nvfp4-low-precision-model-training-for-higher-throughput-without-losing-accuracy/
2. Bridging the Gap — "Bridging the Gap Between Promise and Performance for Microscaling FP4 Quantization", https://arxiv.org/abs/2509.23202
3. NVFP4 Blackwell Comparison — https://huggingface.co/blog/apsys/blackwell-nvfp4-comparison
4. MLPerf Training v5.1 — https://developer.nvidia.com/blog/nvidia-blackwell-architecture-sweeps-mlperf-training-v5-1-benchmarks/
5. FP4 Revolution Overview — https://www.franksworld.com/2025/10/20/the-4-bit-revolution-fp4-training-nvfp4-vs-mxfp4-and-nvidia-blackwell-explained/ (URL corrected 2026-06-12; article verified to exist)
6. NVFP4 Practical Guide — https://zeroshot.it.com/nvfp4-on-blackwell-practical-guide-theory-and-benchmarks-for-4%E2%80%91bit-llms/ (URL corrected 2026-06-12; article verified to exist)
7. AWQ vs GPTQ — https://blog.gopenai.com/awq-vs-gptq-a-practical-decision-framework-for-llm-quantization-e8538e4c486f (URL corrected 2026-06-12; article by Amit Patriwala, verified to exist)
8. LLM Inference Optimization 2026 — https://zylos.ai/research/2026-01-15-llm-inference-optimization/ (verified to exist; source of the "33% faster inference" FP8 figure)
9. Pretraining LLMs with NVFP4 (NVIDIA) — https://arxiv.org/abs/2509.25149 (added 2026-06-12; source for BF16-layer recipe details)
